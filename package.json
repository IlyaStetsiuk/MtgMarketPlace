{
  "name": "mtg-marketplace",
  "version": "1.0.0",
  "private": true,
  "description": "MTG card marketplace with fixed-price listings and live auctions",
  "scripts": {
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm install --include=dev && npm run build",
    "build:backend": "cd backend && npm install --include=dev && npx prisma generate && npx prisma db push --skip-generate && npm run build",
    "start": "cd backend && node dist/index.js"
  }
}
